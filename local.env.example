# Copy to local.env and fill in values (do not commit local.env).
#
# Prefer RTSP if available (H264). HTTP is often MJPEG and would require re-encoding.

DOORBIRD_IP=192.168.11.122
DOORBIRD_RTSP_URL=rtsp://USER:PASS@192.168.11.122/PUT_RTSP_PATH_HERE

